description: 'This fires up a libvirt node which, when joined with a

  kvm-ceph-test-src, will have its qemu instance migrated to it.

  '
name: kvm-ceph-test-dest
provides:
  slave:
    interface: kvm-ceph-test-dest
requires:
  ceph-client:
    interface: ceph-client
summary: Test kvm block migration - destination node
