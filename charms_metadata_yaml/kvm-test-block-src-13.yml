description: 'This fires up a libvirt node with a running qemu instance.

  When a kvm-test-block-dest joins, the instance will be

  migrated.

  '
name: kvm-test-block-src
requires:
  master:
    interface: kvm-test-block-dest
    optional: true
summary: Test kvm block migration - source node
