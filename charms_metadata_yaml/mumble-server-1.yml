description: 'Murmur is the VoIP server component for Mumble. Murmur is installed
  in a

  system-wide fashion, but can also be run by individual users. Each

  murmur process supports multiple virtual servers, each with their own

  user base and channel list.

  '
name: mumble-server
provides:
  voip:
    interface: settings
summary: Low latency VoIP server
